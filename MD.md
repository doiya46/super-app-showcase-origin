# Abc

```sh
git tag -d $(git tag -l)
git push origin --delete $(git tag -l)
```

```sh
git tag booking-android@0.0.1
git tag shopping-android@0.0.1
git tag auth-android@0.0.1
git tag dashboard-android@0.0.1
```
